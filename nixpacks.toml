[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm@9 && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter api run build"]

[start]
cmd = "pnpm --filter api run start:prod"