export const SLOT_GENERATION_DAYS = 30;
export const SLOT_OPEN_HOUR = 6;
export const SLOT_CLOSE_HOUR = 23;
export const SLOT_DURATION_MINUTES = 60;

// Field currently has no price column. Use venue.pricePerHour when present;
// this fallback matches the app's existing mid-range venue pricing convention.
export const DEFAULT_SLOT_PRICE = 500000;

